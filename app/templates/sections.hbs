{{page-title "Sections"}}

<div class="ui green segment">
<LinkTo class="ui positive button" @route="section.add">Nouvelle section</LinkTo>
<LinkTo class="ui positive button" @route="products">Nouveau produit</LinkTo>
</div>
<h2>Store</h2>
<ul>
  {{#each @model as |section|}}
    <li>{{section.name}}
      <LinkTo class="ui mini icon button" @route="section.update" @model={{section.id}}>
        <i class="ui edit icon"></i>
      </LinkTo>
    </li>
  {{/each}}
</ul>

{{outlet}}
