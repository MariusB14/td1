{{page-title "Products"}}

<LinkTo class="ui positive button" @route="products.add">Ajouter un produit</LinkTo>
<ul>
  {{#each @model as |product|}}
    <li>{{product.name}}
      <LinkTo class="ui mini icon button" @route="products.update" @model={{product.id}}>
        <i class="ui edit icon"></i>
      </LinkTo>
    <ul>
      {{#each product.packs as |pack|}}
        <li>{{pack.name}}</li>
      {{/each}}
    </ul>
    </li>
  {{/each}}

</ul>
{{outlet}}
